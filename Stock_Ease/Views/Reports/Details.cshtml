@model Stock_Ease.Models.Report

@{
    ViewData["Title"] = "Report Details";
}

<div class="container-fluid">
    <div class="card mb-4"> 
        <div class="card-body">
            <h1 class="mb-0">@ViewData["Title"]</h1>
        </div>
    </div>

    <div class="card">
        <div class="card-header">
             Details for Report ID: @Model.ReportId
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">
                    @Html.DisplayNameFor(model => model.ReportType)
                </dt>
                <dd class="col-sm-9">
            @Html.DisplayFor(model => model.ReportType)
        </dd>
        <dt class="col-sm-3">
            @Html.DisplayNameFor(model => model.GeneratedDate)
        </dt>
        <dd class="col-sm-9">
            @Model.GeneratedDate.ToString("g")
        </dd>
        <dt class="col-sm-3">
            Generated By
        </dt>
        <dd class="col-sm-9">
            @Html.DisplayFor(model => model.User.Name)
        </dd>
            </dl>
        </div>
        <div class="card-footer d-flex justify-content-start align-items-center"> 
            <a asp-action="Edit" asp-route-id="@Model?.ReportId" class="btn btn-primary">Edit</a>
            <a asp-action="Index" class="btn btn-secondary ms-2">Back to List</a>
        </div>
    </div>
</div>
